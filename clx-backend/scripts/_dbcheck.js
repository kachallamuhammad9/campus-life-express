const c=new (require('pg').Client)({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});(async()=>{await c.connect();const s=await c.query("select inet_server_addr()::text addr, current_database() db, version() v");console.log(JSON.stringify(s.rows[0]));const sc=await c.query("select nspname from pg_namespace where nspname like 'auth%' or nspname like 'storage%' or nspname like 'realtime%'");console.log('schemas:',sc.rows.map(r=>r.nspname).join(','));await c.end();})()
